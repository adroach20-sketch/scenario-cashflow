run = "npm run build && npm start"
entrypoint = "server/index.ts"

[nix]
channel = "stable-24_05"

[deployment]
run = ["sh", "-c", "npm start"]
build = ["sh", "-c", "npm install && npm rebuild better-sqlite3 && npm run build"]
